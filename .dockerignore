# Exclude everything
*

# Only add what is necessary
!package.json
!nest-cli.json
!tsconfig.production.json
!tsconfig.json
!.yarnrc
!yarn.lock
!src
!wallet

# The image tag information is used in the service
!.ci
