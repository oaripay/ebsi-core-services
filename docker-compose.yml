version: "3.7"

services:
  ledger-api:
    build: .
    ports:
      - "${PORT}:${PORT}"
    environment:
      - EBSI_ENV=${EBSI_ENV}
      - API_PRIVATE_KEY=${API_PRIVATE_KEY}
      - BESU_ADDRESS_NOTARY=${BESU_ADDRESS_NOTARY}
      - FABRIC_USERNAME=${FABRIC_USERNAME}
      - FABRIC_PASSWORD=${FABRIC_PASSWORD}
    volumes:
      - ./peerOrganizations:/api/peerOrganizations
