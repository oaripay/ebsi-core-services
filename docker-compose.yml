version: "3.7"

services:
  timestamp-api:
    build: .
    container_name: timestamp-api
    ports:
      - "8080:8080"
    environment:
      - EBSI_ENV=integration
      - BESU_ADDRESS_NOTARY=${BESU_ADDRESS_NOTARY}
