version: "3.7"

services:
  cassandradb:
    image: cassandra:latest
    container_name: cassandradb_test
    ports:
      - "7000:7000"
      - "9042:9042"
    volumes:
      - ./cassandra.yaml:/etc/cassandra/cassandra.yaml
