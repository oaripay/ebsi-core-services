version: "3.7"

services:
  timestamp-api-v5:
    image: timestamp-api-v5
    build: .
    env_file:
      - .env.default
      - .env.default.local
    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"
