query GetUsers($skip: Int!, $pagesize: Int!, $where: User_filter) {
  users(first: $pagesize, skip: $skip, where: $where) {
    id
  }
}

query GetUser($user: ID!) {
  user(id: $user) {
    id
    attributes
  }
}
