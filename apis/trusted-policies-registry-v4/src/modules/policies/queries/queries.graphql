query GetPolicyNames($skip: Int!, $pagesize: Int!) {
  policies(first: $pagesize, skip: $skip) {
    policyName
  }
}

query GetPolicy($policyName: String!) {
  policies(first: 1, where: { policyName: $policyName }) {
    id
    policyName
    description
    status
  }
}
