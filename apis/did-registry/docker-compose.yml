version: "3.7"

services:
  did-registry-api-v3:
    image: did-registry-api-v3
    build: .
    env_file:
      - .env.default
      - .env.local
    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"
