# Default settings for the app
# You can override these settings by creating a new file called .env.default.local

# API domain as per https://ec.europa.eu/digital-building-blocks/wikis/pages/viewpage.action?spaceKey=BLOCKCHAININT&title=Technical+transition+to+the+new+environments
# - localhost (for development purpose only)
# - test.intebsi.xyz
# - api-conformance.ebsi.eu
# - api-pilot.ebsi.[domain]
# - api-preprod.ebsi.[domain]
# - api.ebsi.[domain]
# Format as an uri starting with http(s)
DOMAIN=

# API URL
# API_URL_PREFIX=/storage/v3

# Port on which the server should listen
# Default value: 3000
# Warning: if you override this variable, make sure that docker-compose.yml opens the right port
# API_PORT=

# Authorisation API name (default: authorisation-api)
# AUTHORISATION_API_NAME=

# Supported values : "silent" (turns the logger off) | "error" | "warn" | "info" | "verbose" | "debug"
# Default value: "warn"
# LOG_LEVEL=

# Local API origin (Docker), e.g. "http://api.local"
# When defined, the API will first call this local API before querying the public API if the first request fails
# Default: empty string
# LOCAL_ORIGIN=

# Secret passphrase used to encrypt/decrypt pageState (Cassandra)
ENCRYPTION_SECRET=

# Cassandra user
CASSANDRA_USER=

# Cassandra password
CASSANDRA_PASSWORD=

# Cassandra read consistency level
# Default value: two
# CASSANDRA_CONSISTENCY_READ=

# Cassandra write consistency level
# Default value: two
# CASSANDRA_CONSISTENCY_WRITE=

# Cassandra contact points
# Default value: cassandradb,localhost
# CASSANDRA_CONTACT_POINTS=

# Cassandra local datacenter
# Default value: datacenter1
# CASSANDRA_LOCAL_DATACENTER=

# Cassandra keyspace
CASSANDRA_KEYSPACE=

# Request timeout (number in milliseconds, default: 15000, i.e. 15s)
# Max amount of time a request **made by the API** can take before failing.
# REQUEST_TIMEOUT=
