version: "3.7"

services:
  notifications-api-v2:
    image: notifications-api-v2
    build: .
    env_file:
      - .env.default
      - .env.local
    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"
