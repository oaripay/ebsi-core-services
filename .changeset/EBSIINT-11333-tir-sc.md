---
"@ebsiint-sc/trusted-issuers-registry-v3": patch
---

Restore the original logic of `getIssuer` and `getIssuerAttributeByHash` into `getIssuer__deprecated` and `getIssuerAttributeByHash__deprecated` respectively.
