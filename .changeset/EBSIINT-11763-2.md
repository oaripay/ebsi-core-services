---
"@ebsiint-sc/trusted-schemas-registry-v3": minor
"@ebsiint-api/trusted-schemas-registry-api-v3": minor
---

require schemaId in updateMetadata, allow multiple schemas to share the same content. The contract needs to be deployed on a new address and requires the migration of the existing data.
