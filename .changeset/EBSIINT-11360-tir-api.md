---
"@ebsiint-api/trusted-issuers-registry-api-v5": patch
---

Use the original logic of the `/issuers/:did/attributes/:attributeId/revisions` endpoint when the `version` query parameter is set to `deprecated`.
