export enum Status {
  undefined = "undefined",
  active = "active",
  revoked = "revoked",
  suspended = "suspended",
}

export enum Domain {
  undefined = "undefined",
  ebsi = "ebsi",
  external = "external",
}
