{
  "private": true,
  "name": "@ebsiint-subgraph/subgraphs-deployer",
  "version": "0.0.0-rc.2",
  "type": "module",
  "license": "EUPL-1.2",
  "scripts": {
    "docker:build": "docker build . -t ebsi/subgraphs-deployer:${GIT_COMMIT} --build-arg DOCKER_TAG=${GIT_COMMIT}",
    "setup": "exit 0"
  },
  "dependencies": {
    "@ebsiint-subgraph/core-services": "workspace:*",
    "@ebsiint-subgraph/estat": "workspace:*"
  }
}
