version: "3.7"

services:
  besu:
    build: .
    container_name: besu-api
    ports:
      - ${PORT_BESU}:8080
    environment:
      - EBSI_ENV=${EBSI_ENV}
      - API_PRIVATE_KEY=${API_PRIVATE_KEY}
      - BESU_ADDRESS_NOTARY=${BESU_ADDRESS_NOTARY}
