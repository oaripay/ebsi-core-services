{
  "name": "@ebsi/core-services",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build --all",
    "clean": "nx run-many --target=clean --all",
    "compile": "nx run-many --target=compile --all",
    "lint": "nx run-many --target=lint --all",
    "test": "nx run-many --target=test --all",
    "postinstall": "yarn run clean && yarn run compile"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "nx": "14.4.3",
    "@nrwl/cli": "14.4.3",
    "@nrwl/workspace": "14.4.3",
    "typescript": "~4.7.2",
    "prettier": "^2.6.2"
  },
  "workspaces": [
    "apis/**",
    "contracts/**"
  ]
}
