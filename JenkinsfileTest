pipeline {
    agent any
    stages {
        stage('Setup') {
            steps {
                nodejs(nodeJSInstallationName: '14.17.2') {
                    sh 'yarn install --frozen-lockfile'
                    sh 'yarn run audit'
                }
            }
        }
        stage('Lint') {
            steps {
                nodejs(nodeJSInstallationName: '14.17.2') {
                    sh 'yarn lint'
                }
            }
        }
        stage('Unit tests') {
            environment {
                API_PRIVATE_KEY=credentials('API_PRIVATE_KEY')
            }
            steps {
                nodejs(nodeJSInstallationName: '14.17.2') {
                    sh 'yarn test:ci'
                }
            }
        }
        stage ('Lint Dockerfile') {
            agent {
                docker {
                    image 'hadolint/hadolint:latest-debian'
                    reuseNode true
                }
            }
            steps {
                sh 'hadolint Dockerfile'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('LocalSonar'){
                    sh "/var/lib/jenkins/tools/hudson.plugins.sonar.SonarRunnerInstallation/sonar-scanner/bin/sonar-scanner -Dsonar.host.url=https://infra.ebsi.xyz/sonar -Dsonar.projectName=proxy-data-hub-api -Dsonar.projectVersion=1.0 -Dsonar.projectKey=proxy-data-hub-api -Dsonar.sources=. -Dsonar.projectBaseDir=${WORKSPACE}"
                }
            }
        }
        stage("Quality Gate") {
            steps {
                waitForQualityGate abortPipeline: true
            }
        }
    }
    post {
        always {
            cleanWs()
            dir("${env.WORKSPACE}@script") {
                deleteDir()
            }
        }
    }
}
